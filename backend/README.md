# GeoCities Generator Backend

## Setup

```bash
npm install
```

## Run

```bash
npm run dev
```

Server runs on http://localhost:3001

## API Endpoints

- `POST /api/generate` - Generate a retro webpage
- `POST /api/publish` - Publish a site and get a unique URL
- `GET /site/:siteId` - View a published site
